name: Build and Deploy Blog

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'

      - name: Install Bundler
        run: gem install bundler

      - name: Install dependencies
        run: bundle install
        working-directory: ./blog   # blog フォルダで実行

      - name: Build blog
        run: bundle exec jekyll build -d ./blog_build
        working-directory: ./blog   # blog フォルダで実行

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./blog_build
          publish_branch: gh-pages       # deploy 先
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"
          force_orphan: true             # 初回 push に必要
          allow_empty_commit: true       # 空コミットも許可（初回でファイル生成が遅れる場合に安全）
          commit_message: "Deploy blog via GitHub Actions"



      - name: Check blog_build contents
        run: ls -la ./blog_build
